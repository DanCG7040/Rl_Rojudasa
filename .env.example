# Copia este archivo a .env y rellena los valores.
# En Vercel, configura estas variables en: Project → Settings → Environment Variables

# MongoDB (obligatorio para /api/data.json, /api/save-data y /api/admin/login)
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/

# Nombre de la base de datos (opcional, por defecto: rl-rojudasa)
# MONGODB_DB_NAME=rl-rojudasa
